import React, { Component } from 'react';
export default class component extends Component {
  state = {  }
  render() {
    return (
      <div>
      <button>全部</button>
      <button>未完成</button>
      <button>已完成</button>
      </div>
    );
  }
}